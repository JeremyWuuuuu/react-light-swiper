import * as React from 'react';

export default class ReactLightSwiper extends React.PureComponent<any, any> {
  render() {
    return (
      <div>
        Welcome to React Light Swiper
      </div>
    )
  }
}